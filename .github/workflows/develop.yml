on: 
  push:
    branches:
      - develop
name: 🚀 Deploy to QA
jobs:
  build:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@v3
  
        - name: Build theme
          run: |
            npm install
            npm run production
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Upload ftp
      uses: GenieTim/ftp-action@v4.0.1
      with:
        host: dev.receitasdoparaiso.pt
        user: ${{ secrets.ftp_username }}
        password: ${{ secrets.ftp_password }}
        localDir: "components"
        remoteDir: "/wp-content/themes/receitasdoparaiso-theme/"
        
